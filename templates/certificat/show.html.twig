{% extends 'base.html.twig' %}

{% block title %}Certificat{% endblock %}

{% block body %}
    <h1>Certificat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ certificat.id }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ certificat.score }}</td>
            </tr>
            <tr>
                <th>DateObtention</th>
                <td>{{ certificat.dateObtention ? certificat.dateObtention|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>PdfPath</th>
                <td>{{ certificat.pdfPath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_certificat_index') }}">back to list</a>

    <a href="{{ path('app_certificat_edit', {'id': certificat.id}) }}">edit</a>

    {{ include('certificat/_delete_form.html.twig') }}
{% endblock %}
