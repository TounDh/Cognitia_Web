<form method="post" action="{{ path('dashboard_quiz_delete', {'id': quiz.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce quiz?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quiz.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
