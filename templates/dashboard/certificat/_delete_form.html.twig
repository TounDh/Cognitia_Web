<form method="post" action="{{ path('dashboard_certificat_delete', {'id': certificat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ certificat.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
