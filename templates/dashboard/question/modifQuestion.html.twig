{% extends 'baseDashboard.html.twig' %}

{% block title %}Questions Management!{% endblock %}

{% block body %}

{% if is_granted('ROLE_ADMIN') %}


<div class="content-page">
                <div class="content">

                    <!-- Start Content-->
                    <div class="container-fluid">

                        <div class="row">
                            <div class="col-12">
                                <div class="card">
                                    <div class="card-body">
                                        <h4 class="header-title">Modifier Question</h4>

                                        <div class="row">
                                            <div class="col-lg-6">
                                                {{ form_start(form) }}
                                                <div class="mb-3">
                                                    {{ form_label(form.contenu, 'Contenu de la Question') }}
                                                    {{ form_widget(form.contenu, {'attr': {'class': 'form-control'}}) }}
                                                    {{form_errors(form.contenu)}}
                                                </div>
                                                <div class="mb-3">
                                                    {{ form_label(form.quiz, 'Quiz associé') }}
                                                    {{ form_widget(form.quiz, {'attr': {'class': 'form-control'}}) }}
                                                </div>
                                                <button type="submit" class="btn btn-success">Modifier</button>
                                                {{ form_end(form) }}
                                            <a href="{{ path('dashboard_question_index', {'quiz_id': quiz.id}) }}" class="btn btn-primary mt-3">Retour à la liste Question</a>
                                            </div> <!-- end col -->
                                        </div>
                                        <!-- end row-->

                                    </div> <!-- end card-body -->
                                </div> <!-- end card -->
                            </div><!-- end col -->
                        </div>
                        <!-- end row -->
                    </div> 
                    <!-- end Content-->
                </div>
</div>

{% endif %}

{% endblock %}
