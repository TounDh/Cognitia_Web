{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_commentaire</th>
                <td>{{ commentaire.idCommentaire }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ commentaire.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>Date_actuelle</th>
                <td>{{ commentaire.dateActuelle ? commentaire.dateActuelle|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'id_commentaire': commentaire.id_commentaire}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
